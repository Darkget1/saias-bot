# 심구 알림 명령어 정리

이 문서는 `던컨 / 창백한 산` 심층 구멍 알림 기능의 봇 명령어와 리눅스 서버 운영 명령어를 정리합니다.

## 기본 동작

- 대상 서버: `던컨`
- 대상 지역: `창백한 산`
- 추적 시작: 봇 실행 시 자동 시작
- 기본 체크 간격: `30분`
- 시간 보정: 사이트 응답 시간과 서버 시간이 `60초 이상` 차이 나면 사이트 시간을 기준으로 남은 시간을 계산
- 알림 전송 위치: `/알림선택`으로 지정한 채팅방 1곳
- 알림 재전송: 심구 열림 감지 시 1회, 종료 5분 전 1회
- 전체 채팅방 발송 없음. 지정된 채팅방에만 전송합니다.

## 봇 명령어

| 명령어 | 권한 | 용도 |
| --- | --- | --- |
| `/관리자ID` | 누구나 | 본인 ID와 관리자 여부를 확인합니다. |
| `/심층체크` | 관리자 | 사이트/API 연동 상태를 즉시 확인합니다. |
| `/심구알림시작` | 관리자 | 심구 알림 설정과 현재 추적 상태를 확인합니다. 추적 스레드는 봇 시작 시 자동 실행됩니다. |
| `/알림선택` | 누구나 | 현재 채팅방을 심구 알림방으로 지정합니다. 기존 선택방은 해제되고 현재 채팅방 1곳만 알림방이 됩니다. |
| `/알림선택해제` | 누구나 | 현재 채팅방이 심구 알림방이면 선택을 해제합니다. 다른 방의 선택은 해제하지 않습니다. |

## 명령어 상세

### `/관리자ID`

본인의 `chat.sender.id`를 확인할 때 사용합니다.

예상 출력:

```text
내 ID 확인
────────
• 내 ID: 123456789
• 닉네임: 닉네임
• 권한: 관리자
────────
최초 관리자는 .env에 ADMIN_IDS=123456789 형태로 추가 후 봇을 재시작하세요.
```

### `/심층체크`

관리자 전용입니다.

`mabimobi.life`의 심층 구멍 API에 실제 요청해서 현재 대상 상태를 확인합니다. 연동 실패, 차단, 응답 형식 변경 등이 있으면 실패 메시지가 출력됩니다.

예상 출력:

```text
심층 구멍 체크
대상: 던컨 / 창백한 산
상태: 열림
리셋까지: 13:48
대상 종료까지: 10:00
출현 수: 2
```

상태가 닫힘이면 `대상 종료까지`, `출현 수`는 표시되지 않을 수 있습니다.

사이트 응답 시간과 서버 시간이 60초 이상 차이 나면 다음 줄이 추가됩니다.

```text
시간 보정: +75초
```

### `/심구알림시작`

관리자 전용입니다.

봇 시작 시 자동 실행된 심구 추적 상태와 현재 지정된 알림방을 확인합니다. 알림방이 없으면 `/알림선택`을 먼저 실행하라는 안내를 출력합니다.

예상 출력:

```text
심구 알림 추적 중
대상: 던컨 / 창백한 산
체크 간격: 30분
알림방: 123456789
※ 알림은 지정된 채팅방에만 전송됩니다.

심층 구멍 체크
대상: 던컨 / 창백한 산
상태: 닫힘
리셋까지: 12:34
```

### `/알림선택`

관리자가 아니어도 실행할 수 있습니다.

현재 채팅방을 심구 알림방으로 지정합니다. 알림방은 항상 1곳만 유지되므로, 기존 선택방은 자동 해제됩니다.

예상 출력:

```text
심구 알림 채팅방 선택 완료
선택방: 123456789
대상: 던컨 / 창백한 산
체크 간격: 30분
※ 기존 선택방은 해제되고, 이 채팅방에만 알림을 전송합니다.
```

### `/알림선택해제`

관리자가 아니어도 실행할 수 있습니다.

현재 채팅방이 심구 알림방으로 선택되어 있으면 해제합니다. 현재 채팅방이 선택방이 아니면 아무 설정도 변경하지 않습니다.

예상 출력:

```text
심구 알림 채팅방 선택 해제 완료
해제방: 123456789
※ 이제 이 채팅방에는 심구 알림을 전송하지 않습니다.
```

현재 채팅방이 선택방이 아닐 때:

```text
현재 채팅방은 심구 알림방으로 선택되어 있지 않습니다.
선택방 변경은 알림 받을 채팅방에서 /알림선택 을 실행하세요.
```

## 자동 알림 메시지

심구가 열렸을 때:

```text
창백한 산 지역에 심층 구멍 2개 출현하였습니다. 종료까지 29분 전
```

종료 5분 전:

```text
창백한 산 지역에 심층 구멍 2개 출현하였습니다. 종료까지 5분 전
```

## 리눅스 서버 반영 명령어

서버에서 최신 코드를 받은 뒤 서비스를 재시작합니다.

```bash
cd ~/bot/saias-bot2
git pull
sudo systemctl restart iris2.service
sudo systemctl status iris2.service --no-pager -l
```

최근 로그 확인:

```bash
sudo journalctl -u iris2.service -n 100 --no-pager
```

실시간 로그 확인:

```bash
sudo journalctl -u iris2.service -f
```

서비스가 어떤 파일을 실행하는지 확인:

```bash
systemctl cat iris2.service
```

`ExecStart`가 `irispy.py`를 실행하고 있으면, `sudo systemctl restart iris2.service`만으로 심구 알림 코드가 다시 로드됩니다.

## 배포 전 간단 검증

가상환경을 사용하는 서버라면 먼저 활성화합니다.

```bash
source venv/bin/activate
python -m py_compile bots/deep_hole_alert.py bots/user_system.py
```

문법 에러가 없으면 아무 출력 없이 종료됩니다.

## 관련 파일

- `bots/deep_hole_alert.py`: 심구 API 요청, 상태 파싱, 알림 루프, 알림방 DB 처리
- `bots/user_system.py`: 봇 명령어 처리
- `irispy.py`: 봇 시작 시 `start_lotto_scheduler(bot)`를 호출하며 심구 추적도 함께 시작
